/**
 * Button labels for opening extension HTML surfaces from the dashboard.
 * Aligned with `workspaceModules` titles in `src/shared/config/modules.ts`
 * and with the right-edge navigation dock (Alt+M) surface destinations.
 */
/** integrations.html */
export const OPEN_CONTROL_DECK_LABEL = 'Open Settings';
