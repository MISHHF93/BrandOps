/**
 * Shared responsive width for the mobile shell column (extension tab, Capacitor WebView, narrow browsers).
 * Keeps a comfortable reading width on phones; widens on tablet/desktop like typical chat web apps.
 */
export const MOBILE_SHELL_MAX_WIDTH_CLASS = 'max-w-md sm:max-w-xl md:max-w-2xl lg:max-w-4xl';
