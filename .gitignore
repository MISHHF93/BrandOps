node_modules/

# Generated Vitest / artifact verification outputs (JSON, CSV)
reports/

dist/
release/
*.tsbuildinfo

# Local Vite / dev logs
.vite*.log
.vite-launch*.log
dev-server.log

# Local env (OAuth client IDs, privacy URL, etc.) — not committed
.env.local
.env.*.local

# Windows reserved-name artifact (can't be a real file on disk)
/nul
