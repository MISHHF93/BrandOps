# Loaded only for `npm run dev` — not used by `vite build` (production).
# Dev URL: http://localhost:5173 (strict; stop any other process on 5173 before npm run dev).
# The canonical boot flow is website -> welcome login -> app. Keep the launch sign-in
# gate enforced in dev so the flow matches production (do not set VITE_SKIP_LAUNCH_AUTH).
